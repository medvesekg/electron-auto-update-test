{
  "name": "electron-update-test",
  "version": "1.0.4",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^12.0.4",
    "electron-builder": "^22.10.5"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "electron-updater": "^4.3.8"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/medvesekg/electron-auto-update-test.git"
  }
}
